# YOLO11n with Slim-Neck (based on paper diagram)
nc: 6
depth_multiple: 0.33
width_multiple: 0.25

anchors: 3

# Backbone (保持不变)
backbone:
  - [-1, 1, Conv, [64, 3, 2]]
  - [-1, 1, Conv, [128, 3, 2]]
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]]        # P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]]        # P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]]       # P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]]          # 9
  - [-1, 2, C2PSA, [1024]]            # 10

# Slim-Neck head
head:
  # 上采样: P5 -> P4
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]    # 11
  - [[-1, 6], 1, Concat, [1]]                     # 12
  - [-1, 1, VoV_GSCSP, [512, 1]]                  # 13

  # 上采样: P4 -> P3
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]    # 14
  - [[-1, 4], 1, Concat, [1]]                     # 15
  - [-1, 1, VoV_GSCSP, [256, 1]]                  # 16 (P3-small)

  # 下采样: P3 -> P4
  - [-1, 1, GSConv, [256, 3, 2]]                  # 17
  - [[-1, 13], 1, Concat, [1]]                    # 18
  - [-1, 1, VoV_GSCSP, [512, 1]]                  # 19 (P4-medium)

  # 下采样: P4 -> P5
  - [-1, 1, GSConv, [512, 3, 2]]                  # 20
  - [[-1, 10], 1, Concat, [1]]                    # 21
  - [-1, 1, VoV_GSCSP, [1024, 1]]                 # 22 (P5-large)

  # Detect
  - [[16, 19, 22], 1, Detect, [nc]]               # 23
