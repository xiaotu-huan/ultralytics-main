# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# YOLOv11n 管道缺陷检测（6类）- 修复版
nc: 6
depth_multiple: 0.33
width_multiple: 0.25
anchors: 3

backbone: [
    # P1/2
    [-1, 1, Conv, [64, 3, 2]], # 0-P1/2

    # P2/4
    [-1, 1, Conv, [128, 3, 2]], # 1-P2/4
    [-1, 1, C2f, [128, 1]], # 2-P2/4

    # P3/8
    [-1, 1, Conv, [256, 3, 2]], # 3-P3/8
    [-1, 2, C2f, [256, 1]], # 4-P3/8

    # P4/16 (使用普通C2f先测试)
    [-1, 1, Conv, [512, 3, 2]], # 5-P4/16
    [-1, 2, C2f, [512, 1]], # 6-P4/16

    # P5/32 (使用普通C2f先测试)
    [-1, 1, Conv, [512, 3, 2]], # 7-P5/32
    [-1, 1, C2f, [512, 1]], # 8-P5/32

    # SPPF
    [-1, 1, SPPF, [512, 5]], # 9-P5/32
  ]

head: [
    # P5->P4
    [-1, 1, Conv, [256, 1, 1]], # 10
    [-1, 1, nn.Upsample, [None, 2, "nearest"]], # 11
    [[-1, 6], 1, Concat, [1]], # 12
    [-1, 1, C2f, [256, False]], # 13

    # P4->P3
    [-1, 1, Conv, [128, 1, 1]], # 14
    [-1, 1, nn.Upsample, [None, 2, "nearest"]], # 15
    [[-1, 4], 1, Concat, [1]], # 16
    [-1, 1, C2f, [128, False]], # 17

    # P3->P4
    [-1, 1, Conv, [128, 3, 2]], # 18
    [[-1, 13], 1, Concat, [1]], # 19
    [-1, 1, C2f, [256, False]], # 20

    # P4->P5
    [-1, 1, Conv, [256, 3, 2]], # 21
    [[-1, 10], 1, Concat, [1]], # 22
    [-1, 1, C2f, [512, False]], # 23

    # Detect
    [[17, 20, 23], 1, Detect, [nc]], # 24
  ]
